---
apiVersion: "kubernetes.com/v1"
kind: Network
metadata:
  name: {{ .Release.Name }}-hssdb-net
plugin: bridge
args: '[
    {
        "cniVersion": "0.2.0",
        "bridge": "{{ .Release.Name }}-hssdb-br",
        "name": "{{ .Release.Name }}-hssdb-net",
        "type": "bridge",
        "isGateway": false,
        "ipMasq": true,
        "ipam": {
            "type": "host-local",
            "subnet": "{{ .Values.HSSDBSubnet }}"
        }
    }
]'
