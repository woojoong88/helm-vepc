---
apiVersion: "kubernetes.com/v1"
kind: Network
metadata:
  name: {{ .Release.Name }}-s1u-net
plugin: bridge
args: '[
    {
        "cniVersion": "0.2.0",
        "bridge": "{{ .Release.Name }}-s1u-br",
        "name": "{{ .Release.Name }}-s1u-net",
        "type": "bridge",
        "isGateway": false,
        "ipMasq": true,
        "ipam": {
            "type": "host-local",
            "subnet": "{{ .Values.S1USubnet }}"
        }
    }
]'
