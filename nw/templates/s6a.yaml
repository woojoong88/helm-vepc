---
apiVersion: "kubernetes.com/v1"
kind: Network
metadata:
  name: {{ .Release.Name }}-s6a-net
plugin: bridge
args: '[
    {
        "cniVersion": "0.2.0",
        "bridge": "{{ .Release.Name }}-s6a-br",
        "name": "{{ .Release.Name }}-s6a-net",
        "type": "bridge",
        "isGateway": false,
        "ipMasq": true,
        "ipam": {
            "type": "host-local",
            "subnet": "{{ .Values.S6aSubnet }}"
        }
    }
]'
